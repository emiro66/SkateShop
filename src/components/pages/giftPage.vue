<script setup lang="ts">
// import { onMounted, ref } from 'vue';

// let lastTime = ref(0);

// function gameLoop(currentTime) {
//     // Calculate deltaTime for frame-rate independent updates
//     const deltaTime = (currentTime - lastTime) / 1000; // Convert to seconds
//     lastTime = currentTime;

//     // 1. Handle Input (e.g., keyboard, mouse)
//     handleInput(); 

//     // 2. Update Game Logic (e.g., character movement, physics, AI)
//     updateGameLogic(deltaTime);

//     // 3. Render Scene (e.g., draw to canvas, update DOM elements)
//     renderScene();

//     // Request the next animation frame
//     requestAnimationFrame(gameLoop);
// }

// // Start the game loop
// requestAnimationFrame(gameLoop);

// // Placeholder functions for core game loop phases
// function handleInput() {
//     // Implement input handling logic here
//     // e.g., check for key presses, mouse clicks
// }

// function updateGameLogic(deltaTime) {
//     // Implement game state updates here
//     // e.g., move characters based on deltaTime, check collisions
// }

// function renderScene() {
//     // Implement rendering logic here
//     // e.g., clear canvas, draw sprites, update HTML elements
// }


</script>

<template>

    <p>regerg</p>

</template>
